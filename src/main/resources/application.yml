spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ticket_pro_db?useSSL=false&&allowPublicKeyRetrieval=true&&serverTimezone=Asia/Ho_Chi_Minh
    username: root
    password: 184703
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
server:
  port: 8044